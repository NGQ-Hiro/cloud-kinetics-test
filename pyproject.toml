[project]
name = "cloud-kinetics-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hieu Ngo", email = "hieu.ngo@ascenda.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
